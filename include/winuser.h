/* winuser.h -- lowercase alias for Winuser.h.
 * The documented Header rows print "Winuser.h" (373 rows in the harvest);
 * this alias keeps the lowercase spelling source-compatible for
 * case-sensitive toolchains.  See docs/inventory.md M72. */
#include "Winuser.h"
