/* msgqueue.h -- lowercase alias for Msgqueue.h.
 * The documented Header rows print "Msgqueue.h" (8 rows in the harvest);
 * this alias keeps the lowercase spelling source-compatible for
 * case-sensitive toolchains.  See docs/inventory.md M72. */
#include "Msgqueue.h"
