/* commctrl.h -- lowercase alias for Commctrl.h.
 * The documented Header rows print "Commctrl.h" (805 rows in the harvest);
 * this alias keeps the lowercase spelling source-compatible for
 * case-sensitive toolchains.  See docs/inventory.md M72. */
#include "Commctrl.h"
