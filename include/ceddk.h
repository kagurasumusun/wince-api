/* ceddk.h -- lowercase alias for CEDDK.h.
 * The documented Header rows print "CEDDK.h" (55 rows in the harvest);
 * this alias keeps the lowercase spelling source-compatible for
 * case-sensitive toolchains.  See docs/inventory.md M72. */
#include "CEDDK.h"
