/* Olectl.h -- documented-case alias for Objbase.h.
 * DllRegisterServer (ms864429) and DllUnregisterServer (ms864430) print
 * Header: Olectl.h; both are self-exported by each control DLL --
 * recorded in Objbase.h (M73b), no import declaration. */
#include "Objbase.h"
