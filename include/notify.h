/* notify.h -- lowercase alias for Notify.h.
 * The documented Header rows print "Notify.h" (12 rows in the harvest);
 * this alias keeps the lowercase spelling source-compatible for
 * case-sensitive toolchains.  See docs/inventory.md M72. */
#include "Notify.h"
