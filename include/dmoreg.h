/* dmoreg.h -- lowercase alias for Dmoreg.h.
 * The documented Header rows print "Dmoreg.h" (1 rows in the harvest);
 * this alias keeps the lowercase spelling source-compatible for
 * case-sensitive toolchains.  See docs/inventory.md M72. */
#include "Dmoreg.h"
