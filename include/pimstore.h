/* pimstore.h -- lowercase alias for Pimstore.h.
 * The documented Header rows print "Pimstore.h" (67 rows in the harvest);
 * this alias keeps the lowercase spelling source-compatible for
 * case-sensitive toolchains.  See docs/inventory.md M72. */
#include "Pimstore.h"
