/* wincrypt.h -- lowercase alias for Wincrypt.h.
 * The documented Header rows print "Wincrypt.h" (201 rows in the harvest);
 * this alias keeps the lowercase spelling source-compatible for
 * case-sensitive toolchains.  See docs/inventory.md M72. */
#include "Wincrypt.h"
