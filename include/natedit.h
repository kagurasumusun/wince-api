/* natedit.h -- lowercase alias for Natedit.h.
 * The documented Header rows print "Natedit.h" (20 rows in the harvest);
 * this alias keeps the lowercase spelling source-compatible for
 * case-sensitive toolchains.  See docs/inventory.md M72. */
#include "Natedit.h"
