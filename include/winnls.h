/* winnls.h -- lowercase alias for Winnls.h.
 * The documented Header rows print "Winnls.h" (38 rows in the harvest);
 * this alias keeps the lowercase spelling source-compatible for
 * case-sensitive toolchains.  See docs/inventory.md M72. */
#include "Winnls.h"
