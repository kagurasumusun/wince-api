/* shellapi.h -- lowercase alias for Shellapi.h.
 * The documented Header rows print "Shellapi.h" (16 rows in the harvest);
 * this alias keeps the lowercase spelling source-compatible for
 * case-sensitive toolchains.  See docs/inventory.md M72. */
#include "Shellapi.h"
