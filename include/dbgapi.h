/* dbgapi.h -- lowercase alias for Dbgapi.h.
 * The documented Header rows print "Dbgapi.h" (14 rows in the harvest);
 * this alias keeps the lowercase spelling source-compatible for
 * case-sensitive toolchains.  See docs/inventory.md M72. */
#include "Dbgapi.h"
