/* windef.h -- lowercase alias for Windef.h.
 * The documented Header rows print "Windef.h" (4 rows in the harvest);
 * this alias keeps the lowercase spelling source-compatible for
 * case-sensitive toolchains.  See docs/inventory.md M72. */
#include "Windef.h"
