/* shlobj.h -- lowercase alias for Shlobj.h.
 * The documented Header rows print "Shlobj.h" (14 rows in the harvest);
 * this alias keeps the lowercase spelling source-compatible for
 * case-sensitive toolchains.  See docs/inventory.md M72. */
#include "Shlobj.h"
