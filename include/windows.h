/* windows.h -- lowercase alias for Windows.h.
 * The documented Header rows print "Windows.h" (222 rows in the harvest);
 * this alias keeps the lowercase spelling source-compatible for
 * case-sensitive toolchains.  See docs/inventory.md M72. */
#include "Windows.h"
