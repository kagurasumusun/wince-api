/* celog.h -- lowercase alias for Celog.h.
 * The documented Header rows print "Celog.h" (71 rows in the harvest);
 * this alias keeps the lowercase spelling source-compatible for
 * case-sensitive toolchains.  See docs/inventory.md M72. */
#include "Celog.h"
