/* winldap.h -- lowercase alias for Winldap.h.
 * The documented Header rows print "Winldap.h" (101 rows in the harvest);
 * this alias keeps the lowercase spelling source-compatible for
 * case-sensitive toolchains.  See docs/inventory.md M72. */
#include "Winldap.h"
