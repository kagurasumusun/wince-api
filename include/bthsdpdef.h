/* bthsdpdef.h -- lowercase alias for Bthsdpdef.h.
 * The documented Header rows print "Bthsdpdef.h" (9 rows in the harvest);
 * this alias keeps the lowercase spelling source-compatible for
 * case-sensitive toolchains.  See docs/inventory.md M72. */
#include "Bthsdpdef.h"
