/* dshow.h -- lowercase alias for Dshow.h.
 * The documented Header rows print "Dshow.h" (28 rows in the harvest);
 * this alias keeps the lowercase spelling source-compatible for
 * case-sensitive toolchains.  See docs/inventory.md M72. */
#include "Dshow.h"
