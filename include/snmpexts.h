/* snmpexts.h -- lowercase alias for Snmpexts.h.
 * The documented Header rows print "Snmpexts.h" (3 rows in the harvest);
 * this alias keeps the lowercase spelling source-compatible for
 * case-sensitive toolchains.  See docs/inventory.md M72. */
#include "Snmpexts.h"
