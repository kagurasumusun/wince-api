/* mlang.h -- lowercase alias for Mlang.h.
 * The documented Header rows print "Mlang.h" (107 rows in the harvest);
 * this alias keeps the lowercase spelling source-compatible for
 * case-sensitive toolchains.  See docs/inventory.md M72. */
#include "Mlang.h"
