/* ws2spi.h -- lowercase alias for Ws2spi.h.
 * The documented Header rows print "Ws2spi.h" (40 rows in the harvest);
 * this alias keeps the lowercase spelling source-compatible for
 * case-sensitive toolchains.  See docs/inventory.md M72. */
#include "Ws2spi.h"
