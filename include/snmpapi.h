/* snmpapi.h -- lowercase alias for Snmpapi.h.
 * The documented Header rows print "Snmpapi.h" (1 rows in the harvest);
 * this alias keeps the lowercase spelling source-compatible for
 * case-sensitive toolchains.  See docs/inventory.md M72. */
#include "Snmpapi.h"
