/* windowsx.h -- lowercase alias for Windowsx.h.
 * The documented Header rows print "Windowsx.h" (2 rows in the harvest);
 * this alias keeps the lowercase spelling source-compatible for
 * case-sensitive toolchains.  See docs/inventory.md M72. */
#include "Windowsx.h"
