/* imaging.h -- lowercase alias for Imaging.h.
 * The documented Header rows print "Imaging.h" (97 rows in the harvest);
 * this alias keeps the lowercase spelling source-compatible for
 * case-sensitive toolchains.  See docs/inventory.md M72. */
#include "Imaging.h"
