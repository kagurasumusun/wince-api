/* winscard.h -- lowercase alias for Winscard.h.
 * The documented Header rows print "Winscard.h" (31 rows in the harvest);
 * this alias keeps the lowercase spelling source-compatible for
 * case-sensitive toolchains.  See docs/inventory.md M72. */
#include "Winscard.h"
